#include"main.h"

/**
* mul - that multiplies two integers.
* Return: the multiplication of two numbers
* @a: the input as an integer
* @b: the input as an integer
*/

int mul(int a, int b)
{
	return (a * b);
}
