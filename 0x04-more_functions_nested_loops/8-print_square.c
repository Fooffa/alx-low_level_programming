#include "main.h"

/**
* print_squar - prints a square
*@size : input as an integer
*/

void print_square(int size)
{
	int i, j;

	if (n <= 0)
	_putchar('\n');
	else
	{
		for (j = 0; j < n; j++)
		{
			for (i = 0; i < n; i++)
				_putchar(35);
			_putchar('\n');
		}
	}
}
